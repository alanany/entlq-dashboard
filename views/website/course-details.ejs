<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'تفاصيل الكورس' %> - انطلق</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;800;900&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/feather-icons"></script>
    <style>
        body { font-family: 'Tajawal', sans-serif; }
        .sticky-card {
            top: 2rem;
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-800">

    <header class="bg-white border-b border-gray-200 sticky top-0 z-10">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex justify-between items-center">
            <a href="/" class="text-2xl font-extrabold text-indigo-600">
                <i data-feather="book-open" class="ml-2 w-6 h-6 inline-block"></i> انطلق
            </a>
            <a href="#" onclick="history.back(); return false;" class="text-sm font-semibold text-gray-700 hover:text-indigo-600 transition flex items-center">
                <i data-feather="arrow-right" class="mr-2 w-4 h-4"></i> العودة للكورسات
            </a>
        </div>
    </header>

    <% 
        // الافتراضات الأساسية (ضرورية لعمل EJS)
        
        const levelMap = { 'beginner': 'مبتدئ', 'intermediate': 'متوسط', 'advanced': 'متقدم' };
    %>

    <main>
    <section class="bg-indigo-700 text-white py-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="space-y-4">
                <span class="text-indigo-200 text-sm font-medium tracking-widest uppercase">
                    <%= course.category.name || 'تخصص عام  ' %> | المستوى: <%= levelMap[course.level] || 'غير محدد' %>
                </span>
                <h1 class="text-4xl lg:text-5xl font-extrabold leading-tight"><%= course.title || 'عنوان الكورس غير متوفر' %></h1>
                <p class="text-xl text-indigo-200 max-w-3xl"><%= course.description || 'وصف مختصر للكورس يوضح قيمته المضافة للطالب.' %></p>
                
                <a href="/student/book_plan/<%= course._id %>" 
                   class="inline-flex items-center justify-center px-8 py-3 mt-6 border border-transparent text-xl font-bold rounded-xl shadow-lg text-white bg-green-500 hover:bg-green-600 transition duration-300 transform hover:scale-105">
                    <i data-feather="calendar" class="ml-2 w-6 h-6"></i>
                    احجز الآن
                </a>
                </div>
        </div>
    </section>

    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 lg:py-16">
        <div class="lg:grid lg:grid-cols-12 lg:gap-10">
            
            <div class="lg:col-span-8 space-y-10">
                
                <img src="https://mta.sa/wp-content/uploads/2024/02/12-9-16.png"
                     alt="صورة غلاف الكورس" 
                     class="w-full h-auto max-h-96 object-cover rounded-xl shadow-lg border border-gray-200">

                <div>
                    <h2 class="text-3xl font-bold text-gray-900 mb-4 border-b pb-2">عن الكورس</h2>
                    <div class="prose prose-lg prose-indigo max-w-none text-gray-700">
                        <p><%= course.description || 'لا يوجد وصف تفصيلي لهذا الكورس حالياً.' %></p>
                    </div>
                </div>
                
                <hr class="my-6">
                
                <div>
                    <h2 class="text-3xl font-bold text-gray-900 mb-6">منهج الكورس</h2>
                    
                    <% if (typeof course.curriculum !== 'undefined' && course.curriculum.length > 0) { %>
                        <div class="space-y-4">
                            <% course.curriculum.forEach(function(section, sectionIndex) { %>
                                <details class="bg-white rounded-xl shadow-md border border-indigo-100 open:shadow-lg transition duration-200" open>
                                    <summary class="flex items-center justify-between p-5 cursor-pointer bg-indigo-50 hover:bg-indigo-100 rounded-xl">
                                        <h3 class="text-xl font-bold text-indigo-800">
                                            <span class="ml-3"><%= sectionIndex + 1 %>.</span> <%= section.title %>
                                        </h3>
                                        <span class="text-indigo-600 font-medium text-sm ml-4 flex-shrink-0">
                                            <%= section.lessons.length %> درس
                                        </span>
                                    </summary>
                                    
                                    <ul class="divide-y divide-gray-200 border-t border-indigo-200">
                                        <% section.lessons.forEach(function(lesson) { %>
                                            <li class="flex items-center justify-between p-4 hover:bg-gray-50 transition">
                                                <span class="text-gray-700 flex items-center">
                                                    <i data-feather="video" class="w-4 h-4 ml-2 text-gray-500"></i>
                                                    <%= lesson.title %>
                                                </span>
                                                <span class="text-xs text-indigo-500 font-medium">محتوى خاص</span>
                                            </li>
                                        <% }); %>
                                    </ul>
                                </details>
                            <% }); %>
                        </div>
                    <% } else { %>
                        <div class="text-center p-10 bg-white rounded-xl shadow-md">
                            <p class="text-gray-500">جاري إعداد المنهج التفصيلي لهذا الكورس.</p>
                        </div>
                    <% } %>
                </div>
                
            </div>

            <div class="lg:col-span-4 mt-10 lg:mt-0">
                <div class="sticky sticky-card bg-white p-6 rounded-xl shadow-2xl border-t-4 border-indigo-600">
                    <h3 class="text-2xl font-extrabold text-gray-900 mb-5 border-b pb-3">خيارات الحجز الفردي</h3>

                    <% if (typeof course.pricingOptions !== 'undefined' && course.pricingOptions.length > 0) { %>
                        <div class="space-y-4">
                            <% course.pricingOptions.sort((a, b) => a.duration - b.duration).forEach(function(option) { %>
                                <div class="p-4 border border-indigo-200 rounded-lg flex justify-between items-center bg-indigo-50">
                                    <div>
                                        <p class="text-lg font-bold text-gray-900"><%= option.duration %> دقيقة</p>
                                        <p class="text-sm text-indigo-600">مدة الحصة الواحدة</p>
                                    </div>
                                    <div class="text-left">
                                        <p class="text-2xl font-extrabold text-green-600"><%= option.price %> SAR</p>
                                        <a href="/checkout/<%= course._id %>?duration=<%= option.duration %>" class="text-sm font-medium text-indigo-600 hover:text-indigo-800 transition">
                                            احجز الآن <i data-feather="arrow-left" class="w-3 h-3 inline-block mr-1"></i>
                                        </a>
                                    </div>
                                </div>
                            <% }); %>
                        </div>
                    <% } else { %>
                        <div class="text-center p-4 bg-yellow-50 rounded-lg">
                            <p class="text-yellow-800 text-sm">التسعير غير محدد حالياً. يرجى التواصل معنا.</p>
                        </div>
                    <% } %>

                    <p class="text-center text-xs text-gray-400 mt-6 pt-3 border-t">تتضمن الجدولة المرنة واختيار المعلم.</p>
                </div>
            </div>

        </div>
    </section>

</main>
    <footer class="bg-gray-800 text-white py-10">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <p class="text-sm">&copy; 2025 انطلق. منصة تعليمية عالمية.</p>
        </div>
    </footer>

    <script>
        feather.replace();
    </script>
</body>
</html>