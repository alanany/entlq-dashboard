<style>
    .sidebar {
        width: 260px;
        height: 100vh;
        position: fixed;
        top: 0;
        right: 0;
        background: #fff;
        box-shadow: -5px 0 15px rgba(0,0,0,0.05);
        z-index: 1000;
        transition: all 0.3s;
        display: flex;
        flex-direction: column;
    }

    .sidebar-header {
        padding: 1.5rem;
        text-align: center;
        border-bottom: 1px solid #f1f1f1;
    }

    .nav-link-admin {
        color: #6c757d;
        padding: 0.75rem 1.25rem;
        border-radius: 0.75rem;
        margin: 0.2rem 1rem;
        font-weight: 500;
        transition: all 0.3s;
        display: flex;
        align-items: center;
        font-size: 0.85rem;
        text-decoration: none;
    }

    .nav-link-admin i { 
        margin-left: 1rem; 
        font-size: 1rem;
        width: 20px;
        text-align: center;
    }

    .nav-link-admin:hover, .nav-link-admin.active { 
        color: var(--primary); 
        background-color: var(--secondary); 
    }

    .nav-link-admin.active { font-weight: 700; }

    .group-label-admin {
        font-size: 0.7rem;
        font-weight: 800;
        color: #adb5bd;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin: 1.5rem 1.5rem 0.5rem 0;
        padding-right: 1.25rem;
    }

    .admin-profile-footer {
        padding: 1.25rem;
        border-top: 1px solid #f1f1f1;
        background: #fafbfc;
    }

    @media (max-width: 991.98px) {
        .sidebar { margin-right: -260px; }
        .sidebar.active { margin-right: 0; }
    }
</style>

<aside class="sidebar" id="sidebar">
    <div class="sidebar-header">
        <h3 class="m-0 text-primary lobster-font" style="font-size: 1.8rem;">
            <i class="fa fa-laptop-code me-2"></i><%= t('brand_name') %>
        </h3>
    </div>
    
    <div class="flex-grow-1 overflow-y-auto py-3">
        <a href="/" class="nav-link-admin <%= currentPage === 'home' ? 'active' : '' %>">
            <i class="fa fa-home"></i>
            <span><%= t('home') %></span>
        </a>
        
        <div class="group-label-admin">الإدارة</div>
        
        <a href="/courses" class="nav-link-admin <%= currentPage === 'courses' ? 'active' : '' %>">
            <i class="fa fa-book"></i>
            <span><%= t('courses') %></span>
        </a>
        
        <a href="/subscriptions" class="nav-link-admin <%= currentPage === 'subscriptions' ? 'active' : '' %>">
            <i class="fa fa-file-invoice"></i> 
            <span><%= t('subscriptions') %></span>
        </a>
        
        <a href="/admin/students" class="nav-link-admin <%= currentPage === 'students' ? 'active' : '' %>">
            <i class="fa fa-users"></i>
            <span>الطلاب</span>
        </a>
        
        <a href="/admin/teachers" class="nav-link-admin <%= currentPage === 'teachers' ? 'active' : '' %>">
            <i class="fa fa-user-tie"></i>
            <span>المعلمون</span>
        </a>
        
        <div class="group-label-admin">النظام</div>
        
        <a href="/admin/reports" class="nav-link-admin <%= currentPage === 'reports' ? 'active' : '' %>">
            <i class="fa fa-chart-line"></i>
            <span>التقارير المالية</span>
        </a>
        <a href="/settings" class="nav-link-admin <%= currentPage === 'settings' ? 'active' : '' %>">
            <i class="fa fa-cog"></i>
            <span><%= t('settings') %></span>
        </a>
        
        <div class="group-label-admin">الحساب</div>
        <a href="#" onclick="openLogoutModal(event)" class="nav-link-admin text-danger">
            <i class="fa fa-sign-out-alt"></i>
            <span><%= t('logout') %></span>
        </a>
    </div>

    <div class="admin-profile-footer">
        <div class="d-flex align-items-center gap-3 mb-3">
            <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center fw-bold outfit-font" style="width: 38px; height: 38px; font-size: 0.9rem;">
                <%= user.name.charAt(0).toUpperCase() %>
            </div>
            <div class="overflow-hidden">
                <p class="text-sm fw-bold text-dark mb-0 text-truncate" style="font-size: 0.85rem;"> <%= user.name %> </p>
                <p class="text-muted mb-0 text-truncate" style="font-size: 0.7rem;"><%= user.email %></p>
            </div>
        </div>

        <a href="#" onclick="openLogoutModal(event)" class="btn btn-outline-danger btn-sm w-100 rounded-pill fw-bold border-0 bg-danger bg-opacity-10 py-2">
            <i class="fa fa-sign-out-alt ms-2"></i>
            تسجيل الخروج
        </a>
    </div>
</aside>

<!-- Bootstrap Logout Modal -->
<div class="modal fade" id="logoutModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-sm">
        <div class="modal-content border-0 shadow" style="border-radius: 1.5rem;">
            <div class="modal-body p-4 text-center">
                <div class="bg-danger bg-opacity-10 text-danger mx-auto mb-3 d-flex align-items-center justify-content-center" style="width: 60px; height: 60px; border-radius: 30px; font-size: 1.5rem;">
                    <i class="fa fa-sign-out-alt"></i>
                </div>
                <h5 class="fw-bold mb-2">تأكيد الخروج</h5>
                <p class="text-muted small mb-4">هل أنت متأكد أنك تريد تسجيل الخروج؟</p>
                <div class="d-flex gap-2">
                    <button class="btn btn-light rounded-pill flex-grow-1 small" data-bs-dismiss="modal">إلغاء</button>
                    <a href="/logout" class="btn btn-danger rounded-pill flex-grow-1 small">نعم، اخرج</a>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function openLogoutModal(event) {
        event.preventDefault(); 
        const modal = new bootstrap.Modal(document.getElementById('logoutModal'));
        modal.show();
    }
</script>