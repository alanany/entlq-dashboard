<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تسجيل الدخول - لوحة التحكم</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/feather-icons"></script>
    <style>
        body { font-family: 'Tajawal', sans-serif; }
        /* ضمان أن نص الزر المختار يظهر باللون الأبيض عند التحديد */
        input[type="radio"]:checked + label span {
            color: white !important;
        }
        /* ⭐️ كود الـ Spinner ⭐️ */
        .spinner {
            border: 4px solid rgba(255, 255, 255, 0.3);
            border-top: 4px solid #fff;
            border-radius: 50%;
            width: 1.5rem;
            height: 1.5rem;
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body class="bg-gray-50 flex items-center justify-center min-h-screen text-gray-800">

    <div class="w-full max-w-md">
        <div class="text-center mb-8">
            <h1 class="text-4xl font-extrabold text-indigo-600">أكاديمية انطلق</h1>
            <p class="mt-2 text-xl text-gray-600">لوحة تحكم النظام</p>
        </div>

        <div class="bg-white p-8 rounded-xl shadow-2xl border border-gray-100">
            <h2 class="text-2xl font-bold text-center mb-6">تسجيل الدخول</h2>

            <form onsubmit="handleSubmit(event)" class="space-y-6">
                
                <div class="space-y-2">
                    <label class="block text-sm font-medium text-gray-700">تسجيل الدخول كـ</label>
                    <div class="grid grid-cols-2 gap-3">
                        
                        <div class="relative">
                            <input type="radio" id="role-admin" name="role" value="admin" class="hidden peer" required checked>
                            <label for="role-admin" class="flex flex-col justify-center items-center py-3 px-4 rounded-lg border border-gray-300 cursor-pointer transition-all duration-200 hover:bg-indigo-50 peer-checked:bg-indigo-600 peer-checked:border-indigo-600 shadow-md">
                                <span class="font-bold text-lg text-gray-700 peer-checked:text-white">مدير عام / أدمن</span>
                            </label>
                        </div>
                        
                        <div class="relative">
                            <input type="radio" id="role-instructor" name="role" value="instructor" class="hidden peer">
                            <label for="role-instructor" class="flex flex-col justify-center items-center py-3 px-4 rounded-lg border border-gray-300 cursor-pointer transition-all duration-200 hover:bg-indigo-50 peer-checked:bg-indigo-600 peer-checked:border-indigo-600 shadow-md">
                                <span class="font-bold text-lg text-gray-700 peer-checked:text-white">معلم / مدرب</span>
                            </label>
                        </div>
                    </div>
                </div>
                
                <div>
                    <label for="email" class="block text-sm font-medium text-gray-700">البريد الإلكتروني</label>
                    <input id="email" name="email" type="email" autocomplete="email" required class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm transition-colors">
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700">كلمة المرور</label>
                    <input id="password" name="password" type="password" autocomplete="current-password" required class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm transition-colors">
                </div>

                <div class="flex items-center justify-between">
                    <div class="flex items-center">
                        <input id="remember-me" name="remember-me" type="checkbox" class="h-4 w-4 text-indigo-600 border-gray-300 rounded focus:ring-indigo-500">
                        <label for="remember-me" class="mr-2 block text-sm text-gray-900">تذكرني</label>
                    </div>

                    <div class="text-sm">
                        <a href="#" class="font-medium text-indigo-600 hover:text-indigo-500 transition-colors">هل نسيت كلمة المرور؟</a>
                    </div>
                </div>

                <div>
                    <button id="saveBtn" type="submit" class="w-full flex justify-center items-center py-2 px-4 border border-transparent rounded-lg shadow-sm text-lg font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors">
                        تسجيل الدخول
                        <div id="spinner" class="spinner ml-2 hidden"></div>
                    </button>
                </div>
            </form>
        </div>

        <p class="mt-6 text-center text-sm text-gray-500">
            هذه اللوحة مخصصة للمديرين والمدربين فقط.
            <br>
            <a href="/admin_register" class="font-medium text-indigo-600 hover:text-indigo-500 transition-colors">لإضافة مستخدم جديد، انتقل إلى صفحة التسجيل.</a>
        </p>
    </div>
    
    <div id="toastContainer" class="fixed bottom-4 right-4 z-[70] space-y-3">
    </div>

    <script>
        feather.replace();

        const saveBtn = document.getElementById("saveBtn");
        const spinner = document.getElementById("spinner");
        const originalButtonText = saveBtn.textContent.trim(); 

        // ----------------- دوال التحكم في حالة الزر -----------------

        /**
         * تفعيل وضع التحميل (Loading) على الزر.
         * @param {boolean} isLogin - لتحديد النص (تسجيل الدخول vs إنشاء).
         */
        function startLoading(isLogin = false) {
            saveBtn.disabled = true;
            saveBtn.textContent = isLogin ? 'جاري الدخول...' : 'جاري الإنشاء...';
            spinner.classList.remove("hidden");
        }

        /**
         * إيقاف وضع التحميل وإعادة الزر للحالة الأصلية.
         * @param {boolean} isLogin - لتحديد النص.
         */
        function stopLoading(isLogin = false) {
            saveBtn.disabled = false;
            saveBtn.textContent = originalButtonText; 
            spinner.classList.add("hidden");
        }

        // ----------------- دوال عرض الإشعارات (Toast) -----------------

        /**
         * عرض إشعار منبثق (Toast Notification)
         * @param {string} message - نص الرسالة.
         * @param {string} type - نوع الإشعار: 'success', 'error', 'info'.
         */
        function showToast(message, type = 'info') {
            const container = document.getElementById('toastContainer');
            const toast = document.createElement('div');
            
            let bgColor = 'bg-blue-600';
            let icon = 'info';

            switch (type) {
                case 'success':
                    bgColor = 'bg-green-600';
                    icon = 'check-circle';
                    break;
                case 'error':
                    bgColor = 'bg-red-600';
                    icon = 'alert-triangle';
                    break;
                case 'info':
                default:
                    bgColor = 'bg-blue-600';
                    icon = 'info';
                    break;
            }

            toast.className = `p-4 max-w-xs text-white ${bgColor} rounded-lg shadow-xl flex items-center transform transition-transform duration-300 translate-x-full`;
            toast.innerHTML = `
                <i data-feather="${icon}" class="w-5 h-5 ml-3"></i>
                <span class="text-sm font-medium">${message}</span>
                <button onclick="this.closest('div').remove()" class="mr-2 text-white/80 hover:text-white transition-colors">
                    <i data-feather="x" class="w-4 h-4"></i>
                </button>
            `;
            container.appendChild(toast);
            feather.replace(); 
            
            
            setTimeout(() => {
                toast.classList.remove('translate-x-full');
            }, 10);

            setTimeout(() => {
                toast.classList.add('translate-x-full');
                toast.addEventListener('transitionend', () => toast.remove(), {once: true});
            }, 5000); 
        }

        // ----------------- دالة الإرسال الرئيسية (Handle Submit) -----------------

        /**
         * معالجة إرسال نموذج تسجيل الدخول واستدعاء Fetch API.
         */
        async function handleSubmit(event) {
            event.preventDefault(); 
            
            const form = event.target;
            const userData = {
                role: form.querySelector('input[name="role"]:checked').value,
                email: document.getElementById('email').value,
                password: document.getElementById('password').value
            };

            startLoading(true); // تفعيل وضع التحميل (باستخدام نص تسجيل الدخول)

            try {
                const response = await fetch('/login', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(userData)
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    // طرح كائن الخطأ كاملاً
                    throw errorData; 
                }

                // حالة النجاح:
                const successData = await response.json();
                stopLoading(true);
                
                // عرض رسالة النجاح في Toast
                showToast("تم تسجيل الدخول بنجاح!", 'success'); 
                
                // التحويل إلى لوحة التحكم
                 setTimeout(() => {
                window.location.href = '/'; // تحويل المتصفح إلى صفحة تسجيل الدخول
            }, 800);


            } catch (error) {
                // معالجة الخطأ
                stopLoading(true);

                // 1. محاولة استخلاص رسالة الخطأ الخاصة بالحقول
                const fieldError = 
                    error?.errors?.email || 
                    error?.errors?.password || 
                    error?.errors?.general; 
                
                // 2. استخدام رسالة الخطأ العامة
                const generalError = error?.message || "فشل تسجيل الدخول. تأكد من اتصال الشبكة.";
                
                // 3. الرسالة النهائية
                const finalMessage = fieldError || generalError;

                // عرض الـ Toast
                showToast("خطأ: " + finalMessage, 'error');
            }
        }
        
    </script>
</body>
</html>