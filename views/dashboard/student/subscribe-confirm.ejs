<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تم الاشتراك بنجاح</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;800&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/feather-icons"></script>
    <style>body { font-family: 'Tajawal', sans-serif; }</style>
</head>
<body class="bg-indigo-50 min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-xl w-full text-center space-y-6 p-10 bg-white rounded-xl shadow-2xl border border-gray-100">
        
        <i data-feather="check-circle" class="w-16 h-16 text-green-500 mx-auto mb-4 animate-bounce"></i>
        
        <h1 class="text-3xl font-extrabold text-gray-900">لقد اشتركت بنجاح في الكورس!</h1>
        
        
        <p class="text-gray-700 font-semibold pt-4 border-t border-gray-100">الخطوة التالية: يرجى الانتظار للموافقه والفحص من قبل الادارة .</p>

        <a href="/student/enrolled_subscription" class="w-full md:w-auto inline-flex items-center justify-center px-8 py-3 border border-transparent text-lg font-bold rounded-lg text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-150 shadow-md">
            <i data-feather="calendar" class="ml-3 w-5 h-5"></i>
            الانتقال الى  طلباتى
        </a>
        
     
        
    </div>
    <script>
        feather.replace();

        document.addEventListener('DOMContentLoaded', () => {
            const params = new URLSearchParams(window.location.search);
            const courseId = params.get('course') || 'math';
            
            // في تطبيق حقيقي، ستجلب اسم الكورس من قاعدة البيانات
            const courseNames = { 'math': 'الرياضيات المتقدمة', 'eng': 'اللغة الإنجليزية الأكاديمية', 'cs': 'مقدمة في علوم الحاسب' };
            const name = courseNames[courseId.toLowerCase()] || 'الكورس المحدد';
            
            document.getElementById('subscribedCourseName').textContent = name;
        });
    </script>
</body>
</html>