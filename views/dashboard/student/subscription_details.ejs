<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تفاصيل الطلب</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;800&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/feather-icons"></script>
    <style>body { font-family: 'Tajawal', sans-serif; }</style>
</head>
<body class="bg-gray-100 text-gray-800 min-h-screen">

    <% 
        // 💡 يفترض أن يتم تمرير requestDetails من المتحكم
        const requestDetails = locals.requestDetails; 
        
        // 💡 دالة للحالة
        function getStatusBadge(status) {
            switch(status) {
                case 'pending': return { text: 'قيد المراجعة', color: 'bg-yellow-100 text-yellow-800' };
                case 'confirmed': return { text: 'تمت الموافقة', color: 'bg-indigo-100 text-indigo-800' };
                case 'paid': return { text: 'تم الدفع بنجاح', color: 'bg-green-600 text-white' };
                case 'rejected': return { text: 'مرفوض', color: 'bg-red-100 text-red-800' };
                case 'awaiting_payment': return { text: 'بانتظار الدفع', color: 'bg-blue-100 text-blue-800' };
                default: return { text: 'غير معروف', color: 'bg-gray-100 text-gray-800' };
            }
        }

        const badge = getStatusBadge(requestDetails.status);
        const canViewSessions = requestDetails.status === 'paid' || requestDetails.status === 'confirmed';
    %>

    <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
        
        <div class="mb-4">
            <a href="#" onclick="history.back(); return false;"  class="text-sm text-indigo-600 hover:text-indigo-800 hover:underline flex items-center font-medium transition duration-150">
                <i data-feather="arrow-right" class="mr-1 w-4 h-4"></i> 
                العودة لجميع الطلبات
            </a>
        </div>

        <div class="mb-8 border-b pb-4">
            <h1 class="text-3xl font-extrabold text-gray-900 flex items-center">
                <i data-feather="file-text" class="ml-3 w-7 h-7 text-indigo-600"></i> 
                تفاصيل الطلب (<%= requestDetails._id.toString().slice(0, 8) %>)
            </h1>
            <p class="mt-2 text-lg text-gray-600">تفاصيل انضمامك لكورس **<%= requestDetails.courseId.title %>**.</p>
        </div>
        
        <div class="bg-white shadow-2xl rounded-xl p-6 mb-8 border border-gray-200">
            <h2 class="text-xl font-bold text-indigo-700 mb-4 flex items-center">
                <i data-feather="info" class="ml-2 w-5 h-5"></i> ملخص الطلب والكورس
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 text-sm">
                
                <div>
                    <p class="text-gray-500 font-medium">الكورس</p>
                    <p class="text-gray-900 font-bold"><%= requestDetails.courseId.title %></p>
                </div>
                
                <div>
                    <p class="text-gray-500 font-medium">عدد االحصص بالشهر</p>
                    <p class="text-gray-900"><%= requestDetails.numberOfSessionsPerMonth %></p>
                </div>

                <div>
                    <p class="text-gray-500 font-medium">المبلغ الإجمالي</p>
                    <p class="text-gray-900 font-bold text-lg"><%= requestDetails.totalAmount.toFixed(2) %> ريال</p>
                </div>

                <div>
                    <p class="text-gray-500 font-medium">حالة الطلب</p>
                    <span class="px-3 py-1 inline-flex text-sm leading-5 font-semibold rounded-full <%= badge.color %>">
                        <%= badge.text %>
                    </span>
                </div>
            </div>
            
            <% if (requestDetails.status === 'awaiting_payment') { %>
                <div class="mt-6 p-4 bg-blue-50 border-l-4 border-blue-400 text-blue-800 rounded">
                    <p class="font-bold mb-2">الإجراء المطلوب:</p>
                    <p>يرجى إتمام عملية الدفع لتأكيد مقعدك وبدء عرض جدول الحصص.</p>
                    <a href="/student/pay/<%= requestDetails._id %>" class="mt-3 inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-green-600 hover:bg-green-700 transition">
                        <i data-feather="credit-card" class="ml-2 w-4 h-4"></i> الذهاب لصفحة الدفع
                    </a>
                </div>
            <% } %>
        </div>

        <% if (canViewSessions) { %>
            <div class="bg-white shadow-2xl rounded-xl p-6 border border-gray-200 mt-8">
                <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center">
                    <i data-feather="calendar" class="ml-2 w-6 h-6 text-indigo-600"></i> جدول الحصص والمواعيد
                </h2>
                
                <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-gray-200">
                        <thead class="bg-indigo-50">
                            <tr>
                                <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-indigo-700 uppercase tracking-wider">
                                    # الحصة
                                </th>
                                
                                <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-indigo-700 uppercase tracking-wider">
                                    التاريخ
                                </th>
                                <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-indigo-700 uppercase tracking-wider">
                                    الوقت (بتوقيت الرياض)
                                </th>
                                <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-indigo-700 uppercase tracking-wider">
                                    المدة
                                </th>
                                <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-indigo-700 uppercase tracking-wider">
                                    رابط الانضمام
                                </th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            <% requestDetails.sessions.forEach(function(session, index) { 
// =========================================================
// متغيرات المصدر (يجب أن تكون متوفرة من الباك إند)
// ---------------------------------------------------------
const sessionDateRaw = session.date; // مثال: "2025-12-12T00:00:00.000Z" (تاريخ بدون وقت فعلي)
const sessionTime = session.time;     // مثال: "18:00:00" (الوقت الفعلي للحصة)
// =========================================================

// 1. استخلاص الأجزاء اللازمة من التاريخ الخام (UTC)
const dateObject = new Date(sessionDateRaw);

const year = dateObject.getUTCFullYear();
const month = dateObject.getUTCMonth(); // getUTCMonth() يعطي 0-11
const day = dateObject.getUTCDate();

// 2. استخلاص أجزاء الوقت (المفترض أنه وقت محلي/وقت الحصة الفعلي)
const timeParts = sessionTime.split(':'); 
const hour = parseInt(timeParts[0]);
const minute = parseInt(timeParts[1]);

// 💡 إنشاء كائن التاريخ بوظيفة UTC: setUTCFullYear
// هذا يضمن أن يتم إنشاء الكائن في توقيت UTC مباشرة دون تدخل التوقيت المحلي
const sessionStartTime = new Date();
sessionStartTime.setUTCFullYear(year, month, day);
sessionStartTime.setUTCHours(hour, minute, 0, 0); // تطبيق الساعة والدقيقة بالتوقيت العالمي

// 3. المقارنة الدقيقة
const now = new Date();

// 💡 إنشاء كائن يمثل "الوقت الحالي + 60 دقيقة" (أفضل استخدام UTC أيضاً لضمان الدقة)
const oneHourFromNow = new Date(now.getTime() + (60 * 60 * 1000));

// 4. تحديد ما إذا كانت الحصة قريبة جداً أو قد بدأت
// sessionStartTime.getTime() تكون بالمللي ثانية (مقارنة دقيقة)
const isTooSoon = sessionStartTime.getTime() > oneHourFromNow.getTime(); 

// isReadyOrPassed: هل الحصة ستبدأ خلال الساعة القادمة أو بدأت بالفعل؟
const isReadyOrPassed = sessionStartTime.getTime() <= oneHourFromNow.getTime(); 

// 💡 تعديل مقارنتك لـ isPast:
// isPast: هل وقت بدء الحصة قد مر؟ (أي أن وقت بدء الحصة أقل من الوقت الحالي)
const isPast = sessionStartTime.getTime() < now.getTime();
%>
                                <tr class="<%= isPast ? 'bg-gray-50 text-gray-500' : 'hover:bg-indigo-50' %> transition duration-150">
                                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                                        <%= index+1 %>
                                    </td>
                                   
                                    <td class="px-6 py-4 whitespace-nowrap text-sm font-bold">
                                        <%= sessionDateRaw.toLocaleDateString('ar-EG', { weekday: 'long', year: 'numeric', month: 'numeric', day: 'numeric' }) %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm font-bold">
                                        <%= session.time %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm">
                                        <%= requestDetails.selectedPriceOption %> دقيقة
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">




                                        <% if (!isPast) { %>
                                            <% const bookingId = requestDetails._id; %>
                                            
                                            %>
<a href="/student/session-details/<%= bookingId %>/<%= session._id %>" class="text-green-600 hover:text-green-800 flex items-center font-bold">
             <i data-feather="external-link" class="ml-1 w-4 h-4"></i> الانضمام الآن / قبل الموعد
    </a>

                                          
                                        <% } else { %>
                                            <span class="text-gray-400 flex items-center">
                                                <i data-feather="check-circle" class="ml-1 w-4 h-4"></i> انتهت
                                            </span>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
                
                <% if (requestDetails.sessions.length === 0) { %>
                     <div class="mt-4 p-4 text-center text-gray-500">
                        <i data-feather="alert-circle" class="w-6 h-6 mx-auto mb-2"></i>
                        <p>لم يتم تحديد جدول حصص بعد. يرجى التواصل مع الإدارة.</p>
                    </div>
                <% } %>
            </div>
        <% } else { %>
            <div class="bg-yellow-50 shadow-lg rounded-xl p-6 border-l-4 border-yellow-400 mt-8 text-yellow-800">
                <h2 class="text-xl font-bold flex items-center mb-2">
                    <i data-feather="lock" class="ml-2 w-5 h-5"></i> جدول الحصص غير متاح حالياً
                </h2>
                <p>يظهر جدول الحصص ومواعيدها فقط بعدما يتم **تأكيد دفع** الرسوم أو **الموافقة النهائية** على طلبك.</p>
                <p class="mt-2">الحالة الحالية: **<%= badge.text %>**.</p>
            </div>
        <% } %>

    </div>

    <script>
        // تهيئة أيقونات Feather
        feather.replace();
    </script>
</body>
</html>