<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - لوحة التحكم</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/feather-icons"></script>
    <style>body { font-family: 'Tajawal', sans-serif; }</style>
</head>
<body class="bg-gray-50 text-gray-800">
    <div class="flex h-screen overflow-hidden">
        
     <%- include('./_admin_sidebar.ejs', { currentPage: 'settings', user: user }) %>

        <div class="flex-1 flex flex-col overflow-hidden">
            <header class="h-16 bg-white border-b border-gray-200 flex items-center justify-start px-8">
                <h2 class="text-xl font-bold text-gray-800"><%= title %></h2>
            </header>

            <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-50 p-8">
                <div class="max-w-4xl mx-auto">
                    
                    <h3 class="text-2xl font-bold text-gray-800 mb-6 border-b pb-3">خيارات لوحة التحكم</h3>
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        
                        <a href="/dashboard/settings/account" class="block bg-white p-6 rounded-xl shadow-sm border border-gray-100 hover:shadow-lg transition-shadow duration-300">
                            <i data-feather="user" class="w-8 h-8 text-indigo-500 mb-3"></i>
                            <h4 class="font-bold text-lg text-gray-900 mb-1">إعدادات الحساب</h4>
                            <p class="text-sm text-gray-500">تحديث معلومات الملف الشخصي وبيانات الاتصال.</p>
                        </a>
                        
                        <a href="/categories" class="block bg-white p-6 rounded-xl shadow-sm border border-gray-100 hover:shadow-lg transition-shadow duration-300">
                            <i data-feather="list" class="w-8 h-8 text-green-600 mb-3"></i>
                            <h4 class="font-bold text-lg text-gray-900 mb-1">إدارة الأقسام</h4>
                            <p class="text-sm text-gray-500">إضافة، تعديل، وحذف فئات الدورات الرئيسية.</p>
                        </a>

                        <a href="/dashboard/settings/security" class="block bg-white p-6 rounded-xl shadow-sm border border-gray-100 hover:shadow-lg transition-shadow duration-300">
                            <i data-feather="shield" class="w-8 h-8 text-red-500 mb-3"></i>
                            <h4 class="font-bold text-lg text-gray-900 mb-1">الأمان وكلمة المرور</h4>
                            <p class="text-sm text-gray-500">إدارة كلمة المرور وإعدادات تسجيل الدخول.</p>
                        </a>
                        
                        <a href="/dashboard/settings/system" class="block bg-white p-6 rounded-xl shadow-sm border border-gray-100 hover:shadow-lg transition-shadow duration-300">
                            <i data-feather="sliders" class="w-8 h-8 text-yellow-600 mb-3"></i>
                            <h4 class="font-bold text-lg text-gray-900 mb-1">إعدادات النظام</h4>
                            <p class="text-sm text-gray-500">تكوين العملة، اللغة، والخيارات العامة.</p>
                        </a>

                    </div>
                </div>
            </main>
        </div>
    </div>
    
    <script>
        // لضمان عمل الأيقونات
        feather.replace(); 
    </script>
</body>
</html>